import { PersonalCenterPipe } from './personal-center.pipe';

describe('PersonalCenterPipe', () => {
  it('create an instance', () => {
    const pipe = new PersonalCenterPipe();
    expect(pipe).toBeTruthy();
  });
});
